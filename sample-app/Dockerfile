# DevBox Sample Application — Dockerfile
FROM node:20-alpine

WORKDIR /app

# Copy application source
COPY server.js .

# Expose the container port
EXPOSE 8080

# Start the server
CMD ["node", "server.js"]
